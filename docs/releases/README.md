# Release Notes

Each release gets a versioned note in this directory. Release notes summarize user and operator impact; `CHANGELOG.md` is the concise chronological history.

Use semantic version headings, record the release date, link the relevant guides and ADRs, and distinguish verified capabilities from planned work. Include upgrade notes whenever storage, protocol, configuration, or deployment behavior changes.
