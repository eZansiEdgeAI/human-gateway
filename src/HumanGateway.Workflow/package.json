{
  "name": "@human-gateway/workflow",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "FlowForge HumanInteractionProvider contracts and HumanGateway adapters.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run tests",
    "build": "tsc --noEmit false --outDir dist",
    "headless": "npm run build && node bin/headless.mjs"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vitest": "^4.1.11"
  }
}
